import { createStyles } from "./utils";

export const styles = createStyles({
  header: {
    paddingInline: "0px",
    position: "sticky",
    top: 0,
    zIndex: 1000,
    width: "auto",
    background: "white",
  },
  content: {
    paddingTop: "40px",
  },
});
