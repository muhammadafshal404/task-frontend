import { createStyles } from "../../utils";

export default createStyles({
  titleText: {
    marginTop: "0px",
    marginBottom: "0px",
  },
  columns: {
    display: "flex",
    justifyContent: "center",
  },
  buttonWrapper: {
    display: "flex",
    justifyContent: "end",
  },
});
